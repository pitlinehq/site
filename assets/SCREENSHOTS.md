# Product screenshot provenance

Captured September 7, 2026 from the actual compiled Pitline web interface, using fictional sample sessions served by an isolated static preview. No running daemon, real agent processes, private projects or account data were used.

- `pitline-agents.png`: agents sidebar with sample Claude, Codex and OpenCode sessions; sample Claude approval open.
- `pitline-approval.png`: pending sample command approval.
- `pitline-resumed.png`: response and sample completion after allowing the fixture request.
- `pitline-mobile.png`: the same actual Chat interface at a 390×844 browser viewport.

The sample `acme-web` project, branch names, messages, command and test result are fictional. Screenshots demonstrate the rendered product UI, not a verified vendor run. The homepage labels them as sample sessions. Re-capture against the release build and verify the end-to-end workflow during final release checks.
